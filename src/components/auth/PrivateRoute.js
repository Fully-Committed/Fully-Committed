import React from 'react';
import PropTypes from 'prop-types';

const PrivateRoute = ({ component, patth}) => ();

const loading = useSelector(getL)

PrivateRoute.propTypes = {};

export default PrivateRoute;